apiVersion: v1
kind: Service
metadata:
  name: borrower360
spec:
  type: {{ .Values.service.type }}
  selector:
    app: borrower360
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
